---
import CallToAction from '@components/ui/CallToAction.astro'
import ClickToScroll from '@components/ui/ClickToScroll.astro'
import letters from '@images/assets/aws-letters.png'
import { Picture } from "astro:assets";
import { CTA } from './src/data/constants'
---
<section class="parallax-container" id="como-participar">
  <div class="layer">
    <Picture
      alt="Comunity Day Letras"
      class="image"
      fetchpriority="low"
      formats={['avif', 'webp']}
      height={767}
      src={letters}
      width={1146}
    />
  </div>
  <div class="parallax-cta">
    {
      CTA.map(
        ({ label, description, url, variant, descriptionWeight }, index: number) => (
          <>
            <CallToAction
              callToActionDescription={description}
              callToActionLabel={label}
              callToActionUrl={url}
              descriptionWeight={descriptionWeight}
              variant={variant}
            />
            {index < CTA.length - 1 && <span class="separator" />}
          </>
        )
      )
    }
  </div>
  <ClickToScroll
    href="#informacion"
    label="Ir a la información"
  />
</section>

<style>
  .parallax-container {
    max-width: 1146px;
    margin: auto;
    padding-block: 48px;
    padding-inline: 48px;
    text-align: center;
    position: relative;
    display: flex;
    flex-direction: column;
    gap: 24px;

    @media (width >= 1200px) {
      padding-block: 480px 200px;
      padding-inline: 0;
      gap: 0;
    }

    & .layer {
      img {
        position: absolute;
        inset: 0;
        mask-image: linear-gradient(transparent 0%, var(--black) 30%, var(--black) 80%, transparent 100%);
        z-index: -1;
      }
    }

    & .parallax-cta {
      display: flex;
      justify-content: space-between;
      height: 100%;
      width: 100%;
      max-width: 1280px;
      gap: 1.5rem;
      padding-block: 72px;

      & .separator {
        width: 1px;
        background-color: var(--stone-700);
        margin: 0 1.5rem;
      }
    }
  }

  @media (width <= 1024px) {
    .parallax-container {
      & .layer {
        & img {
          height: 100%;
          object-fit: cover;
          opacity: 0.3;
        }
      }

      & .parallax-cta {
        display: flex;
        flex-direction: column;
        align-items: center;
        padding-block: 48px;

        & .separator {
          width: 100%;
          height: 1px;
          margin: 0;
        }
      }
    }
  }
</style>
